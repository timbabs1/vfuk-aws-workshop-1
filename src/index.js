exports.handler = async (event) => {
    // TODO implement
    const response = {
        statusCode: 200,
        body: JSON.stringify('Hello again last time from my GitHub account!'),
    };
    return response;
};
